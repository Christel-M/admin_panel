<%if logged_in?%>
<div class="forms">
  <div class="forms-grid">
    <div>
      <h1 class="center">Create Cohort</h1>
      <%= form_for([@admin, @cohort]) do |f|%>
      <p><%= f.label :name, "Cohort Name"%> <br>
        <%= f.text_field :name%></p>

        <p><%= f.label :start_date, "Start Date"%> <br>
          <%= f.date_field :start_date%></p>

          <p><%= f.label :end_date, "End Date"%> <br>
            <%= f.date_field :end_date%></p>

            <p><%= f.label :course, "Select Course"%> <br>
              <%= f.select(:course_id, Course.all.collect{ |cr|
                ["#{cr.id}, #{cr.name}"]
                },{include_blank: true })%>
              </p>

              <p><%= f.label :instructor, "Select Instructor"%> <br>
                <%= f.select(:instructor_id, Instructor.all.collect{ |inst|
                  ["#{inst.id}, #{inst.full_name}"]
                  },{include_blank: true })%>
                </p>

                <p>
                  <%= f.collection_select :student_ids, Student.order(:id), :id, :full_name, {}, { multiple: true, class: 'chosen-select' } %>
                </p>


                <p class="center">
                  <%= f.submit "Create Cohort" , class: "btn btn-account"%>
                </p>

                <%end%>
            <div class="center">
                <a class="icons" href="<%= admin_cohorts_path%>">
                  <img src="https://img.icons8.com/bubbles/500/000000/restart.png">
                </a>

                <a class="icons" href="<%= root_path%>">
                  <img src="https://img.icons8.com/bubbles/500/000000/home.png">
                </a>
            </div>
      </div>
    </div>
</div>
<%end%>
